Silhouette is a compiler helper that aims to create an outline of a project in order to only compile what is necessary.

to compile and install on a unix machine, run `make compile && sudo make install`
